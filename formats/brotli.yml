name: brotli
buildsystem: cmake
cleanup:
  - "*.a"
sources:
  - type: git
    url: https://github.com/google/brotli.git
    tag: v1.1.0
    x-checker-data:
      - type: git
        tag-pattern: ^v([\\d.]+)$
